const welcome = (number, groupname) => {
    return `Oiih @${number}. Seja bem_vindo(a) ao grupo ${groupname} leia as regras.`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adeus @${number}. Já vai tarde deve uma mamada para os adm 👋`
}
exports.bye = bye